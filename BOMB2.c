#include<stdio.h>
#include<stdlib.h>

int main()
{
    int N,M,d,c,i,j;
    scanf("%d",&N);
    scanf("%d",&M);

    if(N==0 && M==0) return 0;

    while(M--)
    {
        int p,q;
        scanf("%d %d",&p,&q);
        a[p][q]=1;
    }
    return 0;


}
