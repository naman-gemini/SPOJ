#include<stdio.h>
#include<algorithm>

int main()
{
    int t,n,m,D,i,j,k,flag=0,max=0;
    int hero[500];
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d %d %d",&n,&m,&D);
        while(n--)
        {
            i=0;
            scanf("%d",&hero[i]);
            i++;
        }

        sort(hero);



    }
}
